{
    "name": "tree-sitter-dbdiagram",
    "scripts": {
        "test": "tree-sitter generate && tree-sitter parse example-file.dbdiagram"
    },
    "dependencies": {
        "tree-sitter-cli": "^0.25.4"
    }
}
